# Backend API URL (used by frontend for login/register, meetings, and live transcript WebSocket)
# If backend runs on another port (e.g. 8001), set: VITE_API_URL=http://localhost:8001
VITE_API_URL=http://localhost:8000
